node_modules
dist
.DS_Store

# Capacitor: android-www is regenerated by `npm run cap:prepare`; the android/
# project IS committed (its own android/.gitignore covers build artifacts), but
# the synced web assets under it are regenerated, so keep them out.
android-www
android/app/src/main/assets/public
.gradle

# Built APKs live on the `apk` branch and the GitHub Release, never in source
*.apk
